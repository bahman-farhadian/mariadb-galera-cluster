---
# Inventory is dynamically built from group_vars/all.yml
# This file only contains SSH settings

all:
  vars:
    ansible_user: root
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

# Empty groups to avoid warnings
mariadb:
  hosts: {}
loadbalancers:
  hosts: {}
garbd:
  hosts: {}
